import 'package:flutter/material.dart';

class AreaDivider extends StatelessWidget {
  const AreaDivider({super.key});

  @override
  Widget build(BuildContext context) {
    return Divider(
      indent: 20,
      endIndent: 20,
    );
  }
}
